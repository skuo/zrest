machine:
  java:
    version: oraclejdk8
  timezone:
    UTC
    
dependencies:
  override:
    - mvn dependency:resolve -s conf/settings-circle.xml
    
test:
  override:
    - mvn clean install -s conf/settings-circle.xml -P prod
